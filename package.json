{
  "name": "svelte-parser-ts",
  "version": "1.0.0",
  "description": "Svelte parser for svast ",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "chevrotain": "^7.0.3",
    "typescript": "^4.0.5"
  }
}
